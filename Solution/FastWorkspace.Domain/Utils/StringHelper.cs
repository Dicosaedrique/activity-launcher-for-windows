namespace FastWorkspace.Domain.Utils;

public static class StringHelper
{
    public static string SanatizeString(string str)
    {
        return str; // todo: sanatize string (espace what's necessary, trimming, ...)
    }
}
