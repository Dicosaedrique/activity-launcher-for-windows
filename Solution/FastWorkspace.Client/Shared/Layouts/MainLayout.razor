@inherits LayoutComponentBase

@inject IAppConfiguration AppConfiguration

<MudLayout>
    @if (AppConfiguration.IsAppReady())
    {
        <MudDrawer Open>
            <MudText Typo="Typo.h6" Align="Align.Center" Class="mt-4">Fast Workspace</MudText>
            <NavMenu />
        </MudDrawer>
    }
    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>
