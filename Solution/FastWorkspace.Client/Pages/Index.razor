@inject IAppConfiguration AppConfiguration

@attribute [Route(Routes.Index)]

@if (AppReady)
{
    <Redirect Url="@Routes.HomePage" />
}
else
{
    <Redirect Url="@Routes.ConfigurationPage" />
}

@code {
    private bool AppReady => AppConfiguration.GetFileStorageDirectoryPath() != null;
}
