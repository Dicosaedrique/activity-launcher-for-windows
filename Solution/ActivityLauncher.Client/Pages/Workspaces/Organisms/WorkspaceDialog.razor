@inject WorkspaceController WorkspaceController
@inject IStringLocalizer<AppLocales> Localize

<MudDialog>
    <DialogContent>
        <MudText Typo="Typo.h6" Style="font-size: 1.2rem;">@Localize["Workspace.Dialog.View.Description"]</MudText>
        <MudText Typo="Typo.subtitle1">@Workspace.Description</MudText>
        @* todo: add jobs management *@
    </DialogContent>
</MudDialog>

@code {
    [CascadingParameter]
    public MudDialogInstance MudDialog { get; set; } = null!;

    [Parameter]
    public Workspace Workspace { get; set; } = null!;
}
