@inject ActivityController ActivityController
@inject IStringLocalizer<ActivityLocales> Localize

<GenericTaskCard Task="Task" EditMode="EditMode" OnRemove="OnRemove" HelperTooltip="@Localize["Activity.Dialog.Task.OpenFileTask.HelperTooltip"]" TaskIconSource="/img/OpenFileTaskIcon.png">
    <TaskDetailTemplate>

    </TaskDetailTemplate>
</GenericTaskCard>

@code {
    [Parameter]
    public OpenFileTask Task { get; set; } = null!;

    [Parameter]
    public bool EditMode { get; set; } = false;

    [Parameter]
    public EventCallback OnRemove { get; set; }
}
