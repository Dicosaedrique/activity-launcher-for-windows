@inject ActivityController ActivityController
@inject IStringLocalizer<ActivityLocales> Localize

<GenericTaskCard Task="Task" EditMode="EditMode" OnRemove="OnRemove" HelperTooltip="@Localize["Activity.Dialog.Task.ExplorerFolderTask.HelperTooltip"]" TaskIconSource="/img/ExplorerFolderTaskIcon.png">
    <TaskDetailTemplate>

    </TaskDetailTemplate>
</GenericTaskCard>

@code {
    [Parameter]
    public ExplorerFolderTask Task { get; set; } = null!;

    [Parameter]
    public bool EditMode { get; set; } = false;

    [Parameter]
    public EventCallback OnRemove { get; set; }
}
